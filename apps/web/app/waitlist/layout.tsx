export const metadata = {
  title: "Request beta access",
  description:
    "Join the RaksHex private-beta waitlist. Invited teams receive a scoped evaluation workspace. There is no self-serve checkout.",
  alternates: { canonical: "/waitlist" },
};

export default function WaitlistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
